# Note that modifying any of these settings once any user accounts 
# have been created will certainly break those accounts

# Defines the CPU/memory cost. Must be a power of 2
workFactor=65536

# Defines the size of memory blocks
resources=8

# Defines the cost of parallelisation for an attacker
parallelization=1
